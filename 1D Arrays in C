#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int a[1000],i,add=0,n;
    scanf("%d",&n);    
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
        add += a[i];
        
    }
    printf("%d",add);
    return 0;
}
